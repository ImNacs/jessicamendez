name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to droplet
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > /tmp/key
          chmod 600 /tmp/key
          ssh -o StrictHostKeyChecking=no -i /tmp/key root@165.227.201.91 "cd /root/jessicamendez && git pull origin main"
          rm /tmp/key
