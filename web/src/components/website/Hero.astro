---
/**
 * Hero Section - Astro + CSS Animations
 * Performance-optimized: No JS blocking, CSS-only animations
 */
import { Button } from '@/components/ui/button';
import { profile } from '@/lib/utils';

const credentials = [
  'IFC Performance Standards',
  'Principios de Ecuador',
  'SEMARNAT',
];
---

<section
  id="inicio"
  class="relative min-h-[90vh] flex items-center justify-center overflow-hidden pt-16 pb-12"
>
  <!-- Animated Background -->
  <div class="absolute inset-0 hero-bg-animated" />

  <!-- Morphing Blobs -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="hero-blob hero-blob-verde" />
    <div class="hero-blob hero-blob-rosa" />
  </div>

  <!-- Content -->
  <div class="relative z-10 container mx-auto px-4 text-center max-w-3xl">
    <!-- Credibility badge -->
    <div
      class="inline-flex items-center gap-2 px-4 py-2 bg-verde-50 dark:bg-verde-900/30 border border-verde-200 dark:border-verde-700 rounded-full mb-6 animate-fade-in-up"
      style="animation-delay: 0ms"
    >
      <svg class="h-4 w-4 text-verde-600 dark:text-verde-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z" />
      </svg>
      <span class="text-sm text-verde-700 dark:text-verde-300 font-medium">
        {profile.experience} de experiencia en gestión ambiental
      </span>
    </div>

    <!-- Main heading -->
    <h1
      class="text-[1.75rem] sm:text-4xl md:text-[2.75rem] lg:text-[3.25rem] text-gris-900 dark:text-crema-100 mb-6 animate-fade-in-up"
      style="animation-delay: 100ms"
    >
      Tu aliada en{' '}
      <span class="text-verde-600 dark:text-verde-400">regulacion ambiental</span>{' '}
      y{' '}
      <span class="text-rosa-500 dark:text-rosa-400">financiamiento sostenible</span>
    </h1>

    <!-- Subtitle -->
    <p
      class="text-base sm:text-lg text-gris-600 dark:text-crema-300 mb-6 max-w-2xl mx-auto leading-relaxed animate-fade-in-up"
      style="animation-delay: 200ms"
    >
      Acompaño a desarrolladores de proyectos y entidades financieras en la obtención
      de permisos ambientales y cumplimiento de estándares internacionales.
    </p>

    <!-- Credentials -->
    <div
      class="flex flex-wrap justify-center gap-3 mb-8 animate-fade-in-up"
      style="animation-delay: 300ms"
    >
      {credentials.map((credential) => (
        <div class="inline-flex items-center gap-1.5 px-3 py-1.5 bg-white dark:bg-verde-900/30 border border-gris-200 dark:border-verde-700 rounded-full text-sm text-gris-700 dark:text-crema-200">
          <svg class="h-3.5 w-3.5 text-verde-500 dark:text-verde-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          {credential}
        </div>
      ))}
    </div>

    <!-- CTA Buttons -->
    <div
      class="flex flex-col sm:flex-row gap-3 justify-center animate-fade-in-up"
      style="animation-delay: 400ms"
    >
      <Button
        size="lg"
        className="gap-2 bg-verde-600 hover:bg-verde-700 dark:bg-verde-500 dark:hover:bg-verde-400 text-white dark:text-verde-950 text-base px-8"
      >
        <a href="#contacto" class="flex items-center gap-2">
          Agenda una consulta
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
          </svg>
        </a>
      </Button>
      <Button
        size="lg"
        variant="outline"
        className="border-gris-300 dark:border-verde-600 text-gris-700 dark:text-crema-200 hover:bg-verde-50 dark:hover:bg-verde-900/50 hover:border-verde-400 dark:hover:border-verde-500 hover:text-verde-700 dark:hover:text-verde-300 active:bg-verde-100 dark:active:bg-verde-900 text-base transition-colors"
      >
        <a href="#servicios">Ver servicios</a>
      </Button>
    </div>

    <!-- Social proof -->
    <div
      class="mt-12 pt-8 border-t border-gris-200 dark:border-verde-800 animate-fade-in-up"
      style="animation-delay: 500ms"
    >
      <p class="text-sm text-gris-500 dark:text-crema-400 mb-4">Especialista en sectores de alto impacto</p>
      <div class="flex flex-wrap justify-center gap-x-8 gap-y-2 text-sm font-medium text-gris-600 dark:text-crema-300">
        <span>Energía Renovable</span>
        <span class="hidden sm:inline text-gris-300 dark:text-verde-600">•</span>
        <span>Infraestructura</span>
        <span class="hidden sm:inline text-gris-300 dark:text-verde-600">•</span>
        <span>Minería</span>
        <span class="hidden sm:inline text-gris-300 dark:text-verde-600">•</span>
        <span>Oil & Gas</span>
      </div>
    </div>
  </div>
</section>

<style>
  /* Staggered fade-in animation */
  .animate-fade-in-up {
    opacity: 0;
    animation: fade-in-up 0.6s ease-out forwards;
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
